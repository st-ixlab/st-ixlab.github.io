---
layout: default
---
<div class="post">
  <!-- Full-width profile image at the top (outside container) -->
  {% if page.profile and page.profile.enabled and page.profile.image %}
    <div class="profile-hero-image">
      {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
      {% include figure.liquid loading="eager" path=profile_image_path class="img-fluid" alt=page.profile.image cache_bust=true avoid_scaling=true %}
    </div>
  {% endif %}

  <article>
    <!-- Two-column layout: Lab Intro (50%) + Latest News (50%) -->
    {% assign content_parts = content | split: '<!-- Research Pillars Section -->' %}
    <div class="home-content-layout">
      <div class="home-intro-section">
        <header class="post-header mb-4">
          <h1 class="post-title">
            {% if page.custom_title %}
              {{ page.custom_title }}
            {% elsif site.title == 'blank' %}
              <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
              {{ site.last_name }}
            {% else %}
              {{ site.title }}
            {% endif %}
          </h1>
          <p class="desc">{{ page.subtitle }}</p>
        </header>

        <div class="clearfix">{{ content_parts[0] }}</div>
        
        <img src="{{ '/assets/img/Lab_intro2.png' | relative_url }}" class="img-fluid mt-4 mb-4 d-block mx-auto" alt="Lab Intro 2" style="width: 80%; border-radius: 8px;">
      </div>
      
      <div class="home-news-section">
        {% if page.latest_posts and page.latest_posts.enabled %}
          <h2>
            <a href="{{ '/blog/' | relative_url }}" style="color: inherit">Latest News</a>
          </h2>
          {% include latest_posts.liquid %}
        {% endif %}
      </div>
    </div>

    <!-- Research Pillars Section (from content, after the two-column layout) -->
    {% if content_parts.size > 1 %}
      <div class="research-pillars-section">
        {{ content_parts[1] }}
      </div>
    {% endif %}

    <!-- News -->
    {% if page.announcements and page.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">news</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Location & Sponsors (Moved below Latest News) -->
    <div class="row mt-5">
      <div class="col-md-6">
        <h3>Location</h3>
        <p>Frontier Hall 319-2, Seoul National University of Science and Technology</p>
        <div style="width: 100%; height: 300px; border-radius: 8px; overflow: hidden;">
            <!-- Embedded Google Map for SeoulTech -->
            <iframe src="https://www.google.com/maps/embed?pb=!1m14!1m8!1m3!1d2657.013840759666!2d127.07730825313016!3d37.631808771603176!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x357cbb712d44f303%3A0xf74585f86bbdbe48!2z7ISc7Jq46rO87ZWZ6riw7Iig64yA7ZWZ6rWQIO2UhOuhoO2LsOyWtOq0gA!5e0!3m2!1sen!2skr!4v1767120781643!5m2!1sen!2skr" width="500" height="500" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
        </div>
      </div>
      <div class="col-md-6">
        <h3>Sponsors & Partners</h3>
        <p class="text-muted">Research collaborators and funding agencies.</p>
        <div class="row align-items-center text-center">
            <div class="col-4 mb-4">
                <img src="{{ '/assets/img/partner/seoultech.png' | relative_url }}" alt="SeoulTech" class="img-fluid" style="max-height: 80px;">
            </div>
            <div class="col-4 mb-4">
                <img src="{{ '/assets/img/partner/nrf.png' | relative_url }}" alt="NRF" class="img-fluid" style="max-height: 80px;">
            </div>
            <div class="col-4 mb-4">
                <img src="{{ '/assets/img/partner/lge.png' | relative_url }}" alt="LG Electronics" class="img-fluid" style="max-height: 80px;">
            </div>
            <div class="col-4 mb-4">
                <img src="{{ '/assets/img/partner/kird.png' | relative_url }}" alt="KIRD" class="img-fluid" style="max-height: 80px;">
            </div>
            <div class="col-4 mb-4">
                <img src="{{ '/assets/img/partner/wiset.jpeg' | relative_url }}" alt="WISET" class="img-fluid" style="max-height: 80px;">
            </div>
            <div class="col-4 mb-4">
                <img src="{{ '/assets/img/partner/tie.png' | relative_url }}" alt="TIE" class="img-fluid" style="max-height: 80px;">
            </div>
            <div class="col-4 mb-4">
                <img src="{{ '/assets/img/partner/reco.png' | relative_url }}" alt="TIE" class="img-fluid" style="max-height: 80px;">
            </div>
        </div>
      </div>
    </div>

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">selected publications</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    <!-- Social -->
    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% include social.liquid %}</div>

        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% include newsletter.liquid center=true %}
    {% endif %}
  </article>
</div>
